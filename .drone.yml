kind: pipeline
name: default

steps:
- name: deploy
  image: docker/compose:1.19.0
  volumes:
    - name: cache
      path: /tmp/cache
    - name: conf
      path: /conf
    - name: storage
      path: /storage
  command:
    - docker-compose up
    # - up --help
  
volumes:
- name: conf
  host: 
    path: /var/local/verdaccio/conf
- name: storage
  host: 
    path: /var/local/verdaccio/storage  
- name: cache
  temp: {}


trigger:
  event:
  - push
  branch:
  - develop