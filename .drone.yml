kind: pipeline
name: default

steps:
- name: verdaccio
  image: verdaccio/verdaccio:4
  detach: true
  volumes:
    - /var/local/verdaccio/storage:${pwd}/storage
    - /var/local/verdaccio/conf:${pwd}/conf
  command:
    - ls ${pwd}
  # - docker run -it -d -v /var/local/verdaccio/storage:/verdaccio/storage -v /var/local/verdaccio/conf:/verdaccio/conf --name verdaccio -p 4873:4873 --rm verdaccio/verdaccio:4
  
  


trigger:
  event:
  - push
  branch:
  - develop